{% extends 'base.html.twig' %}

{% block title %}Modifier un type{% endblock %}

{% block body %}
    <h1>Modifier un type de plante</h1>

    {{ include('back/type/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('type_index') }}" class="btn">Retour à la liste</a>

    {{ include('back/type/_delete_form.html.twig') }}
{% endblock %}
