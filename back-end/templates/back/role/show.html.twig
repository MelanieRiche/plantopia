{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ role.name }}</td>
            </tr>
            <tr>
                <th>Role_string</th>
                <td>{{ role.roleString }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('role_index') }}">Retour à la liste</a>

    <a href="{{ path('role_edit', {'id': role.id}) }}">Modifier</a>

    {{ include('back/role/_delete_form.html.twig') }}
{% endblock %}
