import React from 'react';
import TeamCards from 'src/components/About/TeamCards';

import './style.scss';

const About = () => (
  <section className="about">
    <h2 className="signup-title">La Team Plantopia ! </h2>

    <TeamCards />
  </section>
);

export default About;
